# vibecoding大纲

### 初步基础设定：

我想要列出我的游戏大纲，请你根据我这个游戏大纲帮我规划我接下来每一步需要干什么，你将帮助我进行我这个游戏项目的vibecoding。我想要制作一款肉鸽战棋对战的游戏。

1. 首先我需要导入各种英雄的数据（我倾向于使用excel表格然后进行导表工具进行完成） 来完成各个英雄的构建（详细的数据在后续会给出）

2. 游戏的棋盘是每个玩家在3*3的棋盘进行英雄的摆放，玩家有人口限制（先比如说只有5人口）。我需要实现这个基本的功能来进行初步的游戏运行。

   //同时因为是正在开发我也需要我能够有修改器随意的上阵任何英雄和调整我的人口限制

3. 战斗系统的搭建。需要在玩家进行布阵完成之后能够开始战斗，能够与敌方的英雄布阵进行对决。

   //由于我现在是正在开发，我也需要有特定的办法或者修改器来编辑我战斗的敌人的英雄阵容来用于测试。

4. 游戏的运营系统。我决定参考大巴扎。一天有8个回合，普通回合是随机事件（商店，奇遇等）第4、8回合是战斗。其中第4回合的战斗为打野，失败不会有惩罚。第8回合的战斗为与随机玩家的异步战斗（暂时不考虑实现，先用既定的阵容替代），这里失败需要扣除敌方存活人口的生命值，胜利则会获得一个皇冠。
   玩家有15生命值，所有生命值耗尽则游戏结束，或是获得10个皇冠为游戏胜利。

### 进一步UI设定 

//所有帮我构建的UI必须生成于Hierarchy便于我配置修改

建立该画面统领的UI：

1. 在画面右上角放上一个方框用于当指挥官的头像，在头像下方显示玩家血量
2. 在其右侧展示现有金币，下方展示其收入
3. 在画面上方的中央位置展示现在进行到第几天第几回合
4. 在画面的右下角有一个打开仓库的按钮

我想要建立几个用于实现功能的panel：

1. 普通情况下随机的三选一事件，需要三个比较大的矩形方框从上至下依次排放（后续点击事件可能还有什么更多的选项，以后再说先不做）
2. 商店。提供四个道具格子  并且在上方还有一个刷新的按键
3. 仓库。提供跟上阵board一样的board用于担任仓库
4. 战斗。也是提供跟Board一样的敌方阵容，在敌方阵容的右上方也生成一个敌方指挥官的头像，然后用于战斗





### 核心战斗逻辑以及游戏逻辑构建

1. 请先将"D:\_UNITY_Projects\trytryz\文档资料\随从数据表.xlsx"这个是新的随从数据表导入到游戏中，来替换掉之前的所有随从。此外pop指的是人口，替代掉之前的cost

2. 首先我需要将随从实体化：我需要将随从可以自由的在棋盘的格子之间随意的拖拽移动改变位置，拖拽至棋盘位置时会有一定的吸附到棋盘该位置的感觉，请在原本Cell_of_Board预制体的基础上生成随从的预制体。其预制体中需要通过挂载脚本实现包含该随从的基础属性信息（通过id从随从数据表中获取），并且随从的photo需要显示在Cell_of_Board上面。并且在获取随从之后该随从需要变成实体，变成一个独立的随从包含基础属性并且又能记录该随从自己的独立的成长，随从的属性有：随从的原始属性（随从数据表）、获得该随从后该随从永久成长的属性、该随从局内成长的属性（局内成长的属性在战斗结束后消失）。以此实现长线的随从养成效果。

3. 随从数据表解析

   | hp                | atk    | cd/s               | sheld                                | magic_atk                          | crit/%                     | hit/%                               | dodge/%                             | target     |
   | ----------------- | ------ | ------------------ | ------------------------------------ | ---------------------------------- | -------------------------- | ----------------------------------- | ----------------------------------- | ---------- |
   | 生命值，为0则阵亡 | 攻击力 | 每次攻击的时间间隔 | 现有的护盾量，x点护盾可以抵消x点伤害 | 魔法攻击，无视护盾直接造成血量伤害 | 暴击率，暴击造成150%的伤害 | 命中率，真实命中率等于命中率-闪避率 | 闪避率，真实命中率等于命中率-闪避率 | 攻击的目标 |

   其中 target ：最近：指该角色到每个敌方角色位置向量的绝对值最少的目标，如果出现相等最少这两个敌人进行随机。

   直线：优先直线方向上最近，该直线没有敌人之后转为最近。

   随机：随机的敌方随从。

   （其中如何判断最近，则是计算该角色到每个敌方角色位置向量的绝对值。从左上到右下为[1,1]-[3,3] ，与敌方阵容进行对战时敌方的阵容会进行一次左右镜像，此时进行对于距离（即向量绝对值）的计算。例：我方[3,2]是战士，则离敌方[3,2]的距离为1，离敌方[3,1]的距离为√2，即攻击[3,2]敌人。）

4. 战斗详情：我方敌方所有随从同时开始读秒，每个随从在经过自身cd的时间后对攻击目标进行攻击，之后循环往复进行，直到一方随从全部阵亡。每场战斗结束后我方的所有随从的属性回到随从的原始属性+该随从永久成长的属性。

5. 移除Cell_of_Board的UI下方的蓝条，只需要有一个血条就够，并且血条需要变厚（变为原来两倍）并且里面需要显示  当前血量/最大血量 ，并且血条的绿条是按照这个百分比显示 ，并且根据此百分比来决定绿色填充的百分比。在血条的上方添加一个实时显示下一次攻击剩余时间，此外整个随从方块需要按照当前cd进度的百分比从上到下从暗到亮转变：例如刚开始时角色刚开始走cd是全暗的，走了一半cd时上半部分是亮的下半部分是亮的，全走完时全亮并且发动攻击并且重新开始这一循环。发动攻击时随从的方块要往攻击目标的方向震动一下来表示动效。

请你帮我完成我的这些需求

正确的战场：

| [1,1] | [2,1] | [3,1] | [3,1] | [2,1] | [1,1] |
| ----- | ----- | ----- | ----- | ----- | ----- |
| [1,2] | [2,2] | [3,2] | [3,2] | [2,2] | [1,2] |
| [1,3] | [2,3] | [3,3] | [3,3] | [2,3] | [1,3] |



